import asyncio
import logging
import sqlite3
import requests
import pandas as pd
from io import BytesIO
from datetime import datetime, timedelta
from aiogram import Bot, Dispatcher, types, F
from aiogram.filters import CommandStart
from aiogram.fsm.context import FSMContext
from aiogram.fsm.state import State, StatesGroup
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton, InlineKeyboardMarkup, InlineKeyboardButton
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from apscheduler.triggers.cron import CronTrigger

TOKEN = '8521733674:AAGE5DDQFmHYf5O5cLLMom0lpO7nHiCqLuo'

KGEU_API_GROUPS = 'https://kabinet.kgeu.ru/api/raspGroupList'
KGEU_API_SCHEDULE = 'https://kabinet.kgeu.ru/api/Rasp'

logging.basicConfig(level=logging.INFO)
bot = Bot(token=TOKEN)
dp = Dispatcher()
scheduler = AsyncIOScheduler(timezone="Europe/Moscow")

DB_NAME = 'bot_database.db'


def db_start():
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()

    cursor.execute("""CREATE TABLE IF NOT EXISTS users (
        user_id INTEGER PRIMARY KEY,
        first_name TEXT,
        last_name TEXT,
        password TEXT,
        is_logged_in INTEGER DEFAULT 0,
        user_group TEXT DEFAULT '',
        group_id INTEGER DEFAULT 0
    )""")

    connect.commit()
    connect.close()
    print("✅ База данных подключена!")


def add_user_to_db(user_id, first_name, last_name, password):
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("SELECT user_id FROM users WHERE user_id = ?", (user_id,))
    if cursor.fetchone() is None:
        cursor.execute(
            "INSERT INTO users (user_id, first_name, last_name, password, is_logged_in) VALUES (?, ?, ?, ?, ?)",
            (user_id, first_name, last_name, password, 1))
        connect.commit()
        connect.close()
        return True
    connect.close()
    return False


def check_login(user_id, password):
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("SELECT user_id, first_name, last_name FROM users WHERE user_id = ? AND password = ?",
                   (user_id, password))
    user = cursor.fetchone()
    if user:
        cursor.execute("UPDATE users SET is_logged_in = 1 WHERE user_id = ?", (user_id,))
        connect.commit()
        connect.close()
        return True, user[1], user[2]
    connect.close()
    return False, None, None


def logout_user(user_id):
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("UPDATE users SET is_logged_in = 0 WHERE user_id = ?", (user_id,))
    connect.commit()
    connect.close()


def is_user_registered(user_id):
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("SELECT user_id FROM users WHERE user_id = ?", (user_id,))
    result = cursor.fetchone() is not None
    connect.close()
    return result


def get_user_status(user_id):
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("SELECT is_logged_in FROM users WHERE user_id = ?", (user_id,))
    result = cursor.fetchone()
    connect.close()
    return result[0] if result else 0


def save_user_group(user_id, group_name, group_id):
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("UPDATE users SET user_group = ?, group_id = ? WHERE user_id = ?", (group_name, group_id, user_id))
    connect.commit()
    connect.close()


def get_user_data(user_id):
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("SELECT user_group, group_id FROM users WHERE user_id = ?", (user_id,))
    result = cursor.fetchone()
    connect.close()
    return result if result else ('', 0)



def get_all_users_with_groups():
    connect = sqlite3.connect(DB_NAME)
    cursor = connect.cursor()
    cursor.execute("""
        SELECT user_id, user_group, group_id 
        FROM users 
        WHERE group_id > 0 
        AND is_logged_in = 1
    """)
    users = cursor.fetchall()
    connect.close()
    return users


def fetch_schedule_for_today(group_id):
    try:
        headers = {
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
            'Accept': 'application/json',
        }
        url = f'{KGEU_API_SCHEDULE}?idGroup={group_id}'
        response = requests.get(url, headers=headers, timeout=15)
        
        if response.status_code == 200:
            data = response.json()
            if data.get('state') == 1 and data.get('data'):
                return data['data']
        return None
    except Exception as e:
        print(f"Ошибка получения расписания: {e}")
        return None


def group_lessons_by_time(lessons):
    grouped = {}
    for lesson in lessons:
        time_key = (lesson.get('начало', ''), lesson.get('конец', ''))
        if time_key not in grouped:
            grouped[time_key] = []
        grouped[time_key].append(lesson)
    return grouped


def format_today_schedule(schedule_data, group_name):
    if not schedule_data or 'rasp' not in schedule_data:
        return None
    
    rasp = schedule_data['rasp']
    if not rasp:
        return None
    
    today = datetime.now().date()
    today_str = today.strftime('%Y-%m-%d')
    
    days_ru = ['Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота', 'Воскресенье']
    day_name = days_ru[today.weekday()]
    
    today_lessons = []
    for lesson in rasp:
        date_str = lesson.get('дата', '')[:10]
        try:
            lesson_date = None
            for fmt in ['%Y-%m-%d', '%d.%m.%Y', '%d-%m-%Y']:
                try:
                    lesson_date = datetime.strptime(date_str, fmt).date()
                    break
                except ValueError:
                    continue
            
            if lesson_date == today:
                today_lessons.append(lesson)
        except:
            continue
    
    if not today_lessons:
        return None
    
    grouped = group_lessons_by_time(today_lessons)
    
    sorted_times = sorted(grouped.keys(), key=lambda x: x[0])
    
    text = f"🌅 **Доброе утро!**\n\n"
    text += f"📚 **Расписание на сегодня**\n"
    text += f"📅 {today.strftime('%d.%m.%Y')} ({day_name})\n"
    text += f"👥 Группа: **{group_name}**\n\n"
    
    pair_num = 1
    for time_key in sorted_times:
        time_start, time_end = time_key
        lessons_at_time = grouped[time_key]
        
        text += f"**{pair_num}. {time_start} - {time_end}**\n"
        
        if len(lessons_at_time) == 1:
            lesson = lessons_at_time[0]
            discipline = lesson.get('дисциплина', '')
            teacher = lesson.get('преподаватель', '')
            room = lesson.get('аудитория', '')
            lesson_type = lesson.get('вид', '')
            subgroup = lesson.get('подгруппа', '')
            
            text += f"📖 {discipline}\n"
            if lesson_type:
                text += f"📝 {lesson_type}\n"
            if subgroup:
                text += f"👥 Подгруппа: {subgroup}\n"
            text += f"👨‍🏫 {teacher}\n"
            text += f"🚪 Ауд. {room}\n\n"
        else:
            for idx, lesson in enumerate(lessons_at_time):
                discipline = lesson.get('дисциплина', '')
                teacher = lesson.get('преподаватель', '')
                room = lesson.get('аудитория', '')
                lesson_type = lesson.get('вид', '')
                subgroup = lesson.get('подгруппа', '')
                
                if idx > 0:
                    text += f"  ─────\n"
                
                text += f"📖 {discipline}\n"
                if lesson_type:
                    text += f"📝 {lesson_type}\n"
                if subgroup:
                    text += f"👥 Подгруппа: {subgroup}\n"
                else:
                    text += f"👥 Вариант {idx + 1}\n"
                text += f"👨‍🏫 {teacher}\n"
                text += f"🚪 Ауд. {room}\n"
            text += "\n"
        
        pair_num += 1
    
    text += f"\n_Всего пар: {len(sorted_times)}_"
    
    return text


async def send_daily_schedule():
    print(f"🕐 [{datetime.now()}] Запуск рассылки расписания...")
    
    users = get_all_users_with_groups()
    print(f"📊 Найдено пользователей для рассылки: {len(users)}")
    
    if not users:
        print("⚠️ Нет пользователей для рассылки")
        return
    
    groups_cache = {}
    sent_count = 0
    error_count = 0
    no_schedule_count = 0
    
    for user_id, group_name, group_id in users:
        try:
            if group_id not in groups_cache:
                schedule = fetch_schedule_for_today(group_id)
                groups_cache[group_id] = schedule
            else:
                schedule = groups_cache[group_id]
            
            if schedule:
                schedule_text = format_today_schedule(schedule, group_name)
                
                if schedule_text:
                    await bot.send_message(
                        chat_id=user_id,
                        text=schedule_text,
                        parse_mode="Markdown"
                    )
                    sent_count += 1
                    print(f"✅ Отправлено пользователю {user_id} ({group_name})")
                else:
                    today = datetime.now()
                    days_ru = ['Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота', 'Воскресенье']
                    day_name = days_ru[today.weekday()]
                    
                    no_lessons_text = (
                        f"🌅 **Доброе утро!**\n\n"
                        f"📅 {today.strftime('%d.%m.%Y')} ({day_name})\n"
                        f"👥 Группа: **{group_name}**\n\n"
                        f"🎉 **Сегодня нет занятий!**\n\n"
                        f"Хорошего отдыха! 😊"
                    )
                    await bot.send_message(
                        chat_id=user_id,
                        text=no_lessons_text,
                        parse_mode="Markdown"
                    )
                    no_schedule_count += 1
            else:
                error_count += 1
                print(f"⚠️ Не удалось получить расписание для группы {group_name}")
            
            # Небольшая задержка между отправками чтобы не превысить лимиты Telegram
            await asyncio.sleep(0.1)
            
        except Exception as e:
            error_count += 1
            print(f"❌ Ошибка отправки пользователю {user_id}: {e}")
    
    print(f"📊 Итоги рассылки: отправлено {sent_count}, без занятий {no_schedule_count}, ошибок {error_count}")



def fetch_groups_from_api():
    try:
        headers = {
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
            'Accept': 'application/json',
        }
        response = requests.get(KGEU_API_GROUPS, headers=headers, timeout=15)
        if response.status_code == 200:
            data = response.json()
            return data.get('data', [])
        return []
    except Exception as e:
        print(f"Ошибка получения групп: {e}")
        return []


def search_groups(groups, query):
    query = query.upper().strip()
    found = []
    for group in groups:
        if query in group['name'].upper():
            found.append(group)
    return found[:20]


def fetch_schedule_from_api(group_id):
    try:
        headers = {
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
            'Accept': 'application/json',
        }
        url = f'{KGEU_API_SCHEDULE}?idGroup={group_id}'
        response = requests.get(url, headers=headers, timeout=15)
        
        if response.status_code == 200:
            data = response.json()
            if data.get('state') == 1 and data.get('data'):
                return data['data']
        return None
    except Exception as e:
        print(f"Ошибка получения расписания: {e}")
        return None



def get_schedule_dates(schedule_data):
    if not schedule_data or 'rasp' not in schedule_data:
        return []
    
    rasp = schedule_data['rasp']
    if not rasp:
        return []
    
    dates = set()
    for lesson in rasp:
        date_str = lesson.get('дата', '')[:10]
        try:
            for fmt in ['%Y-%m-%d', '%d.%m.%Y', '%d-%m-%Y']:
                try:
                    lesson_date = datetime.strptime(date_str, fmt).date()
                    dates.add(lesson_date)
                    break
                except ValueError:
                    continue
        except:
            continue
    
    return sorted(list(dates))


def format_schedule_for_date(schedule_data, group_name, target_date):
    if not schedule_data or 'rasp' not in schedule_data:
        return None
    
    rasp = schedule_data['rasp']
    if not rasp:
        return None
    
    days_ru = ['Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота', 'Воскресенье']
    day_name = days_ru[target_date.weekday()]
    
    day_lessons = []
    for lesson in rasp:
        date_str = lesson.get('дата', '')[:10]
        try:
            lesson_date = None
            for fmt in ['%Y-%m-%d', '%d.%m.%Y', '%d-%m-%Y']:
                try:
                    lesson_date = datetime.strptime(date_str, fmt).date()
                    break
                except ValueError:
                    continue
            
            if lesson_date == target_date:
                day_lessons.append(lesson)
        except:
            continue
    
    if not day_lessons:
        return None
    
    grouped = group_lessons_by_time(day_lessons)
    
    sorted_times = sorted(grouped.keys(), key=lambda x: x[0])
    
    today = datetime.now().date()
    if target_date == today:
        relative = "сегодня"
    elif target_date == today + timedelta(days=1):
        relative = "завтра"
    elif target_date == today - timedelta(days=1):
        relative = "вчера"
    else:
        relative = ""
    
    text = f"📚 **Расписание на {target_date.strftime('%d.%m.%Y')}**\n"
    text += f"📅 {day_name}"
    if relative:
        text += f" ({relative})"
    text += f"\n👥 Группа: **{group_name}**\n\n"
    
    pair_num = 1
    for time_key in sorted_times:
        time_start, time_end = time_key
        lessons_at_time = grouped[time_key]
        
        text += f"**{pair_num}. {time_start} - {time_end}**\n"
        
        if len(lessons_at_time) == 1:
            lesson = lessons_at_time[0]
            discipline = lesson.get('дисциплина', '')
            teacher = lesson.get('преподаватель', '')
            room = lesson.get('аудитория', '')
            lesson_type = lesson.get('вид', '')
            subgroup = lesson.get('подгруппа', '')
            
            text += f"📖 {discipline}\n"
            if lesson_type:
                text += f"📝 {lesson_type}\n"
            if subgroup:
                text += f"👥 Подгруппа: {subgroup}\n"
            text += f"👨‍🏫 {teacher}\n"
            text += f"🚪 Ауд. {room}\n\n"
        else:
            for idx, lesson in enumerate(lessons_at_time):
                discipline = lesson.get('дисциплина', '')
                teacher = lesson.get('преподаватель', '')
                room = lesson.get('аудитория', '')
                lesson_type = lesson.get('вид', '')
                subgroup = lesson.get('подгруппа', '')
                
                if idx > 0:
                    text += f"  ─────\n"
                
                text += f"📖 {discipline}\n"
                if lesson_type:
                    text += f"📝 {lesson_type}\n"
                if subgroup:
                    text += f"👥 Подгруппа: {subgroup}\n"
                else:
                    text += f"👥 Вариант {idx + 1}\n"
                text += f"👨‍🏫 {teacher}\n"
                text += f"🚪 Ауд. {room}\n"
            text += "\n"
        
        pair_num += 1
    
    text += f"_Всего пар: {len(sorted_times)}_"
    
    return text


def get_week_days_keyboard(schedule_data, current_week_offset=0):
    today = datetime.now().date()
    
    week_start = today - timedelta(days=today.weekday()) + timedelta(weeks=current_week_offset)
    
    days_ru_short = ['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс']
    
    available_dates = set(get_schedule_dates(schedule_data)) if schedule_data else set()
    
    keyboard = []
    row = []
    
    for i in range(7):
        day_date = week_start + timedelta(days=i)
        day_str = day_date.strftime('%d.%m')
        day_name = days_ru_short[i]
        
        has_lessons = day_date in available_dates
        
        if day_date == today:
            btn_text = f"📍 {day_name} {day_str}"
        elif has_lessons:
            btn_text = f"📚 {day_name} {day_str}"
        else:
            btn_text = f"➖ {day_name} {day_str}"
        
        row.append(InlineKeyboardButton(
            text=btn_text,
            callback_data=f"schedule_day_{day_date.strftime('%Y-%m-%d')}"
        ))
        
        if len(row) == 2:
            keyboard.append(row)
            row = []
    
    if row:
        keyboard.append(row)
    
    nav_row = []
    if current_week_offset > -2:  # Не больше 2 недель назад
        nav_row.append(InlineKeyboardButton(
            text="⬅️ Пред. неделя",
            callback_data=f"schedule_week_{current_week_offset - 1}"
        ))
    
    if current_week_offset < 4:  # Не больше 4 недель вперед
        nav_row.append(InlineKeyboardButton(
            text="След. неделя ➡️",
            callback_data=f"schedule_week_{current_week_offset + 1}"
        ))
    
    if nav_row:
        keyboard.append(nav_row)
    
    if current_week_offset != 0:
        keyboard.append([InlineKeyboardButton(
            text="📍 Вернуться к текущей неделе",
            callback_data="schedule_week_0"
        )])
    
    keyboard.append([InlineKeyboardButton(text="❌ Закрыть", callback_data="close_schedule")])
    
    return InlineKeyboardMarkup(inline_keyboard=keyboard)


def get_day_navigation_keyboard(current_date_str, week_offset=0):
    current_date = datetime.strptime(current_date_str, '%Y-%m-%d').date()
    prev_date = current_date - timedelta(days=1)
    next_date = current_date + timedelta(days=1)
    
    keyboard = [
        [
            InlineKeyboardButton(
                text="⬅️ Пред. день",
                callback_data=f"schedule_day_{prev_date.strftime('%Y-%m-%d')}"
            ),
            InlineKeyboardButton(
                text="След. день ➡️",
                callback_data=f"schedule_day_{next_date.strftime('%Y-%m-%d')}"
            )
        ],
        [InlineKeyboardButton(
            text="📅 Выбрать другой день",
            callback_data=f"schedule_week_{week_offset}"
        )],
        [InlineKeyboardButton(text="❌ Закрыть", callback_data="close_schedule")]
    ]
    
    return InlineKeyboardMarkup(inline_keyboard=keyboard)



class AuthState(StatesGroup):
    reg_name = State()
    reg_surname = State()
    reg_password = State()
    login_password = State()


class GroupState(StatesGroup):
    search_group = State()


def get_main_menu():
    return ReplyKeyboardMarkup(keyboard=[
        [KeyboardButton(text="📝 Регистрация"), KeyboardButton(text="🔑 Вход")],
        [KeyboardButton(text="🚪 Выход"), KeyboardButton(text="ℹ️ О боте")]
    ], resize_keyboard=True)


def get_logged_menu():
    # УБРАНА кнопка "🔔 Уведомления" - уведомления приходят всем автоматически
    return ReplyKeyboardMarkup(keyboard=[
        [KeyboardButton(text="👥 Выбрать группу"), KeyboardButton(text="📅 Моё расписание")],
        [KeyboardButton(text="📅 На сегодня")],
        [KeyboardButton(text="📱 Мобильное приложение"), KeyboardButton(text="🌐 Сайт")],
        [KeyboardButton(text="🚪 Выход"), KeyboardButton(text="ℹ️ О боте")]
    ], resize_keyboard=True)


def get_cancel_kb():
    return InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="❌ Отмена", callback_data="cancel_process")]
    ])


def get_back_kb():
    return InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="🔙 Назад", callback_data="back_to_menu")]
    ])


def get_search_results_keyboard(groups):
    keyboard = []
    for group in groups:
        keyboard.append([InlineKeyboardButton(
            text=f"{group['name']} ({group['kurs']} курс)",
            callback_data=f"select_group_{group['id']}"
        )])
    
    keyboard.append([InlineKeyboardButton(text="❌ Отмена", callback_data="cancel_process")])
    return InlineKeyboardMarkup(inline_keyboard=keyboard)


def get_app_links_keyboard():
    return InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="🍎 App Store", url="https://apps.apple.com/ru/app/кгэу-расписание/id1544568777")],
        [InlineKeyboardButton(text="📱 Google Play", url="https://play.google.com/store/apps/details?id=ru.kgeu.rasp")],
        [InlineKeyboardButton(text="🔷 AppGallery", url="https://appgallery.huawei.com/app/C103667171")]
    ])



@dp.message(CommandStart())
async def cmd_start(message: types.Message, state: FSMContext):
    await state.clear()
    user_id = message.from_user.id
    if is_user_registered(user_id):
        if get_user_status(user_id) == 1:
            text = "👋 С возвращением!\n\n🚀 Бот КГЭУ Расписание готов к работе!\n\n🔔 Расписание на текущий день приходит автоматически в 07:00"
            await message.answer(text, parse_mode="Markdown", reply_markup=get_logged_menu())
        else:
            text = "👋 Вы зарегистрированы, но не вошли.\nНажмите '🔑 Вход'."
            await message.answer(text, parse_mode="Markdown", reply_markup=get_main_menu())
    else:
        text = "👋 Добро пожаловать!\nНажмите '📝 Регистрация' для старта."
        await message.answer(text, parse_mode="Markdown", reply_markup=get_main_menu())


@dp.message(F.text == "📝 Регистрация")
async def btn_register(message: types.Message, state: FSMContext):
    await state.clear()
    if is_user_registered(message.from_user.id):
        await message.answer("⚠️ Вы уже зарегистрированы!", reply_markup=get_main_menu())
        return
    await state.set_state(AuthState.reg_name)
    await message.answer("📝 Введите **Имя**:", parse_mode="Markdown", reply_markup=get_cancel_kb())


@dp.message(AuthState.reg_name)
async def process_reg_name(message: types.Message, state: FSMContext):
    await state.update_data(name=message.text)
    await state.set_state(AuthState.reg_surname)
    await message.answer("📝 Введите **Фамилию**:", parse_mode="Markdown", reply_markup=get_cancel_kb())


@dp.message(AuthState.reg_surname)
async def process_reg_surname(message: types.Message, state: FSMContext):
    await state.update_data(surname=message.text)
    await state.set_state(AuthState.reg_password)
    await message.answer("🔒 Придумайте **Пароль** (мин. 4 знака):", parse_mode="Markdown", reply_markup=get_cancel_kb())


@dp.message(AuthState.reg_password)
async def process_reg_password(message: types.Message, state: FSMContext):
    if len(message.text) < 4:
        await message.answer("⚠️ Короткий пароль! Попробуйте еще раз:")
        return
    data = await state.get_data()
    success = add_user_to_db(message.from_user.id, data['name'], data['surname'], message.text)
    await state.clear()
    if success:
        await message.answer(f"✅ **Успешно!**\n\n🚀 Выберите группу для получения расписания\n\n🔔 Расписание на текущий день будет приходить автоматически в 07:00", parse_mode="Markdown",
                             reply_markup=get_logged_menu())
    else:
        await message.answer("⚠️ Ошибка.", reply_markup=get_main_menu())


@dp.message(F.text == "🔑 Вход")
async def btn_login(message: types.Message, state: FSMContext):
    await state.clear()
    if not is_user_registered(message.from_user.id):
        await message.answer("⚠️ Сначала зарегистрируйтесь!", reply_markup=get_main_menu())
        return
    if get_user_status(message.from_user.id) == 1:
        await message.answer("✅ Вы уже вошли!", reply_markup=get_logged_menu())
        return
    await state.set_state(AuthState.login_password)
    await message.answer("🔒 Введите **Пароль**:", parse_mode="Markdown", reply_markup=get_cancel_kb())


@dp.message(AuthState.login_password)
async def process_login_password(message: types.Message, state: FSMContext):
    success, name, surname = check_login(message.from_user.id, message.text)
    await state.clear()
    if success:
        await message.answer(f"✅ **Вход выполнен!**\n{name} {surname}\n\n🚀 **Выберите раздел:**", parse_mode="Markdown",
                             reply_markup=get_logged_menu())
    else:
        await message.answer("❌ Неверный пароль.", reply_markup=get_main_menu())


@dp.message(F.text == "🚪 Выход")
async def btn_logout(message: types.Message, state: FSMContext):
    await state.clear()
    logout_user(message.from_user.id)
    await message.answer("🚪 Вы вышли из аккаунта.", reply_markup=get_main_menu())


@dp.message(F.text == "📅 На сегодня")
async def btn_today_schedule(message: types.Message):
    user_id = message.from_user.id
    group_name, group_id = get_user_data(user_id)
    
    if not group_id:
        await message.answer("⚠️ Сначала выберите группу! Нажмите '👥 Выбрать группу'", reply_markup=get_logged_menu())
        return
    
    await message.answer("⏳ Загружаю расписание на сегодня...")
    schedule = fetch_schedule_for_today(group_id)
    
    if schedule:
        schedule_text = format_today_schedule(schedule, group_name)
        if schedule_text:
            schedule_text = schedule_text.replace("🌅 **Доброе утро!**\n\n", "")
            await message.answer(schedule_text, parse_mode="Markdown", reply_markup=get_logged_menu())
        else:
            today = datetime.now()
            days_ru = ['Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота', 'Воскресенье']
            day_name = days_ru[today.weekday()]
            await message.answer(
                f"📅 {today.strftime('%d.%m.%Y')} ({day_name})\n"
                f"👥 Группа: **{group_name}**\n\n"
                f"🎉 **Сегодня нет занятий!**",
                parse_mode="Markdown",
                reply_markup=get_logged_menu()
            )
    else:
        await message.answer("⚠️ Не удалось получить расписание.\nПопробуйте позже.", reply_markup=get_logged_menu())



@dp.message(F.text == "👥 Выбрать группу")
async def btn_select_group(message: types.Message, state: FSMContext):
    await state.set_state(GroupState.search_group)
    await message.answer(
        "🔍 **Введите название вашей группы**\n\n"
        "Например: `ЭС-1-23` или `АТП-2-22`\n\n"
        "Можно ввести часть названия для поиска:",
        parse_mode="Markdown",
        reply_markup=get_cancel_kb()
    )


@dp.message(GroupState.search_group)
async def process_group_search(message: types.Message, state: FSMContext):
    query = message.text.strip()
    
    if len(query) < 2:
        await message.answer("⚠️ Введите минимум 2 символа для поиска", reply_markup=get_cancel_kb())
        return
    
    await message.answer("⏳ Ищу группу...")
    
    groups = fetch_groups_from_api()
    if not groups:
        await message.answer("⚠️ Не удалось загрузить список групп. Попробуйте позже.", reply_markup=get_logged_menu())
        await state.clear()
        return
    
    found_groups = search_groups(groups, query)
    
    if not found_groups:
        await message.answer(
            f"❌ Группа **{query}** не найдена\n\n"
            "Попробуйте ввести другое название:",
            parse_mode="Markdown",
            reply_markup=get_cancel_kb()
        )
        return
    
    if len(found_groups) == 1:
        group = found_groups[0]
        save_user_group(message.from_user.id, group['name'], group['id'])
        await state.clear()
        await message.answer(
            f"✅ Группа **{group['name']}** выбрана!",
            parse_mode="Markdown",
            reply_markup=get_logged_menu()
        )
    else:
        await state.update_data(found_groups=found_groups)
        await message.answer(
            f"🔍 Найдено групп: {len(found_groups)}\n\nВыберите вашу группу:",
            reply_markup=get_search_results_keyboard(found_groups)
        )



@dp.message(F.text == "📅 Моё расписание")
async def btn_my_schedule(message: types.Message, state: FSMContext):
    user_id = message.from_user.id
    group_name, group_id = get_user_data(user_id)

    if not group_id:
        await message.answer("⚠️ Сначала выберите группу! Нажмите '👥 Выбрать группу'", reply_markup=get_logged_menu())
        return

    await message.answer("⏳ Загружаю расписание...")
    schedule = fetch_schedule_from_api(group_id)
    
    if not schedule:
        await message.answer("⚠️ Не удалось получить расписание.\nПопробуйте позже.", reply_markup=get_logged_menu())
        return
    
    await state.update_data(schedule_data=schedule, group_name=group_name, group_id=group_id)
    
    today = datetime.now().date()
    week_start = today - timedelta(days=today.weekday())
    week_end = week_start + timedelta(days=6)
    
    await message.answer(
        f"📚 **Расписание группы {group_name}**\n\n"
        f"📅 Неделя: {week_start.strftime('%d.%m')} - {week_end.strftime('%d.%m.%Y')}\n\n"
        f"📍 - сегодня\n"
        f"📚 - есть занятия\n"
        f"➖ - нет занятий\n\n"
        f"**Выберите день:**",
        parse_mode="Markdown",
        reply_markup=get_week_days_keyboard(schedule, 0)
    )


@dp.callback_query(F.data.startswith("schedule_week_"))
async def schedule_week_navigation(callback: types.CallbackQuery, state: FSMContext):
    """Навигация по неделям"""
    week_offset = int(callback.data.split("_")[2])
    
    data = await state.get_data()
    schedule = data.get('schedule_data')
    group_name = data.get('group_name', '')
    
    if not schedule:
        group_id = data.get('group_id')
        if group_id:
            schedule = fetch_schedule_from_api(group_id)
            await state.update_data(schedule_data=schedule)
    
    today = datetime.now().date()
    week_start = today - timedelta(days=today.weekday()) + timedelta(weeks=week_offset)
    week_end = week_start + timedelta(days=6)
    
    if week_offset == 0:
        week_label = "Текущая неделя"
    elif week_offset == 1:
        week_label = "Следующая неделя"
    elif week_offset == -1:
        week_label = "Прошлая неделя"
    else:
        week_label = f"Неделя {week_offset:+d}"
    
    await callback.message.edit_text(
        f"📚 **Расписание группы {group_name}**\n\n"
        f"📅 {week_label}: {week_start.strftime('%d.%m')} - {week_end.strftime('%d.%m.%Y')}\n\n"
        f"📍 - сегодня\n"
        f"📚 - есть занятия\n"
        f"➖ - нет занятий\n\n"
        f"**Выберите день:**",
        parse_mode="Markdown",
        reply_markup=get_week_days_keyboard(schedule, week_offset)
    )


@dp.callback_query(F.data.startswith("schedule_day_"))
async def schedule_day_selected(callback: types.CallbackQuery, state: FSMContext):
    date_str = callback.data.split("_")[2]
    selected_date = datetime.strptime(date_str, '%Y-%m-%d').date()
    
    data = await state.get_data()
    schedule = data.get('schedule_data')
    group_name = data.get('group_name', '')
    
    if not schedule:
        await callback.answer("⚠️ Данные устарели. Нажмите '📅 Моё расписание' заново.", show_alert=True)
        return
    
    schedule_text = format_schedule_for_date(schedule, group_name, selected_date)
    
    if schedule_text:
        await callback.message.edit_text(
            schedule_text,
            parse_mode="Markdown",
            reply_markup=get_day_navigation_keyboard(date_str)
        )
    else:
        days_ru = ['Понедельник', 'Вторник', 'Среда', 'Четверг', 'Пятница', 'Суббота', 'Воскресенье']
        day_name = days_ru[selected_date.weekday()]
        
        await callback.message.edit_text(
            f"📅 **{selected_date.strftime('%d.%m.%Y')}** ({day_name})\n"
            f"👥 Группа: **{group_name}**\n\n"
            f"🎉 **В этот день нет занятий!**",
            parse_mode="Markdown",
            reply_markup=get_day_navigation_keyboard(date_str)
        )


@dp.callback_query(F.data == "close_schedule")
async def close_schedule(callback: types.CallbackQuery, state: FSMContext):
    await state.clear()
    await callback.message.edit_text("✅ Расписание закрыто")
    await callback.message.answer("🏠 Главное меню:", reply_markup=get_logged_menu())


# ============ ОСТАЛЬНЫЕ ОБРАБОТЧИКИ ============

@dp.message(F.text == "📱 Мобильное приложение")
async def btn_mobile_app(message: types.Message, state: FSMContext):
    await message.answer(
        "📱 **Мобильное приложение КГЭУ Образование**\n\n"
        "Скачайте приложение для вашего устройства:",
        parse_mode="Markdown",
        reply_markup=get_app_links_keyboard()
    )


@dp.message(F.text == "🌐 Сайт")
async def btn_site(message: types.Message):
    await message.answer(
        "🌐 **Сегодня будет доступен**\n\n"
        "",
        parse_mode="Markdown"
    )


@dp.callback_query(F.data.startswith("select_group_"))
async def select_group(callback: types.CallbackQuery, state: FSMContext):
    group_id = int(callback.data.split("_")[2])
    
    data = await state.get_data()
    groups = data.get('found_groups') or fetch_groups_from_api()
    
    group = next((g for g in groups if g['id'] == group_id), None)

    if group:
        save_user_group(callback.from_user.id, group['name'], group['id'])
        await state.clear()
        await callback.message.edit_text(f"✅ Группа **{group['name']}** выбрана!", parse_mode="Markdown")
        await callback.message.answer("🏠 Главное меню:", reply_markup=get_logged_menu())
    else:
        await callback.answer("⚠️ Ошибка выбора группы", show_alert=True)


@dp.callback_query(F.data == "cancel_process")
async def cancel_process(callback: types.CallbackQuery, state: FSMContext):
    await state.clear()
    await callback.message.edit_text("❌ Отменено.")
    user_status = get_user_status(callback.from_user.id)
    await callback.message.answer("Выберите действие:",
                                  reply_markup=get_logged_menu() if user_status == 1 else get_main_menu())


@dp.callback_query(F.data == "back_to_menu")
async def back_to_menu(callback: types.CallbackQuery, state: FSMContext):
    await state.clear()
    await callback.message.answer("🏠 Главное меню:", reply_markup=get_logged_menu())


@dp.message()
async def echo_all(message: types.Message):
    pass


async def main():
    db_start()
    
    # Настраиваем планировщик на 07:00 каждый день
    scheduler.add_job(
        send_daily_schedule,
        CronTrigger(hour=0, minute=40, timezone="Europe/Moscow"),
        id="daily_schedule",
        replace_existing=True
    )
    scheduler.start()
    print("⏰ Планировщик запущен! Рассылка в 07:00 по МСК")
    
    print("🚀 Бот запущен...")
    await dp.start_polling(bot)


if __name__ == "__main__":
    try:
        asyncio.run(main())
    except KeyboardInterrupt:
        print("Бот выключен")



код востановление базыд данных
#!/usr/bin/env python3
"""
🔧 ПРОДВИНУТЫЙ СКРИПТ ВОССТАНОВЛЕНИЯ БАЗЫ ДАННЫХ SQLite
Поместите этот файл в папку с вашим ботом (где находится bot_database.db)
и запустите: python recover_database_advanced.py
"""

import sqlite3
import os
import shutil
import subprocess
from datetime import datetime

DB_NAME = 'bot_database.db'
# Перед удалением базы
if os.path.exists(DB_NAME):
    try:
        # Попытка закрыть соединение, если оно осталось открытым
        conn = sqlite3.connect(DB_NAME)
        conn.close()
        # Временно удаляем файл для восстановления
        os.remove(DB_NAME)
    except Exception as e:
        print(f"❗ Не удалось закрыть или удалить базу: {e}")
        # Попытка завершить работу с помощью системных команд
        # или вывести сообщение, что нужно закрыть все процессы, использую
def method1_direct_read():
    """Метод 1: Прямое чтение данных"""
    print("\n🔹 Метод 1: Прямое чтение...")
    
    try:
        conn = sqlite3.connect(DB_NAME)
        conn.execute("PRAGMA journal_mode=OFF")
        cursor = conn.cursor()
        
        cursor.execute("SELECT * FROM users")
        data = cursor.fetchall()
        
        cursor.execute("PRAGMA table_info(users)")
        columns = [col[1] for col in cursor.fetchall()]
        
        conn.close()
        
        if data:
            print(f"   ✅ Извлечено {len(data)} записей")
            return data, columns
        
    except Exception as e:
        print(f"   ❌ Ошибка: {e}")
    
    return [], []

def method2_sqlite3_recover():
    """Метод 2: Использование команды .recover через sqlite3"""
    print("\n🔹 Метод 2: SQLite3 .recover...")
    
    try:
        # Проверяем наличие sqlite3
        result = subprocess.run(
            ['sqlite3', DB_NAME, '.recover'],
            capture_output=True,
            text=True,
            timeout=60
        )
        
        if result.returncode == 0 and result.stdout:
            # Сохраняем SQL дамп
            with open('recovery_dump.sql', 'w', encoding='utf-8') as f:
                f.write(result.stdout)
            print(f"   ✅ SQL дамп сохранён в recovery_dump.sql")
            return True
        else:
            print(f"   ⚠️ Ошибка: {result.stderr}")
            
    except FileNotFoundError:
        print("   ⚠️ sqlite3 не найден в системе")
    except Exception as e:
        print(f"   ❌ Ошибка: {e}")
    
    return False

def method3_page_by_page():
    """Метод 3: Постраничное чтение"""
    print("\n🔹 Метод 3: Постраничное чтение...")
    
    all_data = []
    
    try:
        conn = sqlite3.connect(DB_NAME)
        cursor = conn.cursor()
        
        offset = 0
        limit = 10
        
        while True:
            try:
                cursor.execute(f"SELECT * FROM users LIMIT {limit} OFFSET {offset}")
                rows = cursor.fetchall()
                
                if not rows:
                    break
                
                all_data.extend(rows)
                offset += limit
                
            except Exception as e:
                print(f"   ⚠️ Ошибка на offset {offset}: {e}")
                offset += limit
                if offset > 10000:  # Защита от бесконечного цикла
                    break
        
        conn.close()
        
        if all_data:
            print(f"   ✅ Извлечено {len(all_data)} записей")
            return all_data
            
    except Exception as e:
        print(f"   ❌ Ошибка: {e}")
    
    return []

def create_fresh_database(data=None):
    """Создание новой базы данных и восстановление данных"""
    print("\n📋 Создание новой базы данных...")
    
    # Создаём резервную копию
    timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
    backup_name = f'bot_database_corrupted_{timestamp}.db'
    
    if os.path.exists(DB_NAME):
        shutil.copy2(DB_NAME, backup_name)
        print(f"   📦 Резервная копия: {backup_name}")
        os.remove(DB_NAME)
    
    # Создаём новую базу
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    
    cursor.execute("""CREATE TABLE IF NOT EXISTS users (
        user_id INTEGER PRIMARY KEY,
        first_name TEXT,
        last_name TEXT,
        password TEXT,
        is_logged_in INTEGER DEFAULT 0,
        user_group TEXT DEFAULT '',
        group_id INTEGER DEFAULT 0
    )""")
    
    conn.commit()
    print(f"   ✅ Создана новая база: {DB_NAME}")
    
    # Восстанавливаем данные
    if data:
        restored = 0
        for row in data:
            try:
                if len(row) >= 7:
                    cursor.execute("""
                        INSERT OR REPLACE INTO users 
                        (user_id, first_name, last_name, password, is_logged_in, user_group, group_id)
                        VALUES (?, ?, ?, ?, ?, ?, ?)
                    """, row[:7])
                    restored += 1
                elif len(row) >= 5:
                    cursor.execute("""
                        INSERT OR REPLACE INTO users 
                        (user_id, first_name, last_name, password, is_logged_in)
                        VALUES (?, ?, ?, ?, ?)
                    """, row[:5])
                    restored += 1
            except Exception as e:
                print(f"   ⚠️ Пропущена запись: {e}")
        
        conn.commit()
        print(f"   ✅ Восстановлено записей: {restored}")
    
    conn.close()
    return True

def verify_database():
    """Проверка целостности базы"""
    print("\n📋 Проверка базы данных...")
    
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    
    cursor.execute("PRAGMA integrity_check")
    result = cursor.fetchone()
    
    if result[0] == 'ok':
        print("   ✅ Целостность: OK")
    else:
        print(f"   ⚠️ Проблемы: {result}")
    
    cursor.execute("SELECT COUNT(*) FROM users")
    count = cursor.fetchone()[0]
    print(f"   📊 Записей в таблице users: {count}")
    
    if count > 0:
        cursor.execute("SELECT user_id, first_name, last_name, user_group FROM users LIMIT 5")
        rows = cursor.fetchall()
        print("   📋 Первые записи:")
        for row in rows:
            print(f"      {row}")
    
    conn.close()

def main():
    print("=" * 60)
    print("🔧 ВОССТАНОВЛЕНИЕ БАЗЫ ДАННЫХ SQLite")
    print("=" * 60)
    
    if not os.path.exists(DB_NAME):
        print(f"\n❌ Файл {DB_NAME} не найден!")
        print("   Создаю новую пустую базу...")
        create_fresh_database()
        verify_database()
        return
    
    # Пробуем разные методы восстановления
    extracted_data = []
    
    # Метод 1
    data, columns = method1_direct_read()
    if data:
        extracted_data = data
    
    # Метод 3 если метод 1 не сработал
    if not extracted_data:
        extracted_data = method3_page_by_page()
    
    # Метод 2 (дополнительно)
    method2_sqlite3_recover()
    
    # Создаём новую базу
    create_fresh_database(extracted_data)
    
    # Проверяем
    verify_database()
    
    print("\n" + "=" * 60)
    print("✅ ВОССТАНОВЛЕНИЕ ЗАВЕРШЕНО!")
    print("=" * 60)
    
    if extracted_data:
        print(f"\n📊 Восстановлено {len(extracted_data)} пользователей")
    else:
        print("\n⚠️ Данные не удалось извлечь.")
        print("   Если у вас есть резервные копии, восстановите их вручную.")
    
    print("\n🚀 Теперь можете запустить бота!")

if __name__ == "__main__":
    main()
bot_database.db
SQLite format 3   @                                                                     .�
   � �                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  �
�ytableusersusersCREATE TABLE users (
        user_id INTEGER PRIMARY KEY,
        first_name TEXT,
        last_name TEXT,
        password TEXT,
        is_logged_in INTEGER DEFAULT 0,
        user_group TEXT DEFAULT '',
        group_id INTEGER DEFAULT 0
    )
       
bot_database_corrupted_20260219_213016.db
SQLite format 3   @     @                                                               @ .�
   � �                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  �
�ytableusersusersCREATE TABLE users (
        user_id INTEGER PRIMARY KEY,
        first_name TEXT,
        last_name TEXT,
        password TEXT,
        is_logged_in INTEGER DEFAULT 0,
        user_group TEXT DEFAULT '',
        group_id INTEGER DEFAULT 0
    )
� 
����!
g1
� c
�S��
�sg
$*���                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          4����: %%	РанельКулиевNefedoni5ПЭ-1-240�5���� -)	СамандарЭргашев0411ПЭ-1-240�8����< '5	🔑 ВходПанкратова7891ПЭ-1-240�1��ۂ' %)	МаксимБаташевMaksПЭ-1-240�.����$ )	ФролАнтиповPizzaПЭ-1-240�-����8 )	ПупаЛупович1234ПЭ-1-240�0�0���� '%	🔑 ВходГатина2006ПЭ-1-240�+����U %	СашаЗубова1258ХТ-1-250�I�ꖘ( 111	ℹ️ О ботеℹ️ О ботеℹ️ О ботеПЭ-1-250�7����B %-	'ПолинаПетухова2107ПЭИС-2-250�=��� 15	ЕкатеринаГригорьева3112ПЭ-1-250�=���� A!	РимВалимухаметовNezabydy68ПЭ-1-250�=���. 15	АлександрИгольченко2513ПЭ-1-250�7�Ӧ�V --	ВикторияГабышева0605ПЭ-1-250�C���� C)	Разиля МежноваМежноваRazilyaПЭ-1-250�1�޷�U !-	ДианаЛисицина1480ПЭ-1-250�� � 0�ޔ�I %%	СтепанКнязев5337!ПЭ-1-250�   �B %-	Полин���� 	хт7890ХТ-1-250�<����	 !5#	ЗахарПерепелкинVolchara18&ПЭ-1-250�-����d -	ПодВрр а про1234ПЭ-1-250�9����. %1	ИльсафГатауллинmama4422ПЭ-1-250�%���0 	варвввр1234ПЭ-1-250�

C:\Users\Владимир\PycharmProjects\PythonProject12\.venv\Scripts\python.exe C:\Users\Владимир\PycharmProjects\PythonProject12\112.py 
Traceback (most recent call last):
  File "C:\Users\Владимир\PycharmProjects\PythonProject12\112.py", line 1020, in <module>
    asyncio.run(main())
    ~~~~~~~~~~~^^^^^^^^
  File "C:\Users\Владимир\AppData\Local\Python\pythoncore-3.14-64\Lib\asyncio\runners.py", line 204, in run
    return runner.run(main)
           ~~~~~~~~~~^^^^^^
  File "C:\Users\Владимир\AppData\Local\Python\pythoncore-3.14-64\Lib\asyncio\runners.py", line 127, in run
    return self._loop.run_until_complete(task)
           ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~^^^^^^
  File "C:\Users\Владимир\AppData\Local\Python\pythoncore-3.14-64\Lib\asyncio\base_events.py", line 719, in run_until_complete
    return future.result()
           ~~~~~~~~~~~~~^^
  File "C:\Users\Владимир\PycharmProjects\PythonProject12\112.py", line 1002, in main
    db_start()
    ~~~~~~~~^^
  File "C:\Users\Владимир\PycharmProjects\PythonProject12\112.py", line 33, in db_start
    cursor.execute("""CREATE TABLE IF NOT EXISTS users (
    ~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
        user_id INTEGER PRIMARY KEY,
        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    ...<5 lines>...
        group_id INTEGER DEFAULT 0
        ^^^^^^^^^^^^^^^^^^^^^^^^^^
    )""")
    ^^^^^
sqlite3.DatabaseError: database disk image is malformed

Process finished with exit code 1
нужно из базы данных bot_database_corrupted_20260219_213016.db чтобы бот использовал данные 
